version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:10

    working_directory: ~/repo

    steps:
      - checkout
      - run:
          name: install yarn
          command: 'sudo npm install -g yarn --quiet'
      - run:
          name: prebuild, load dependencies
          command: 'yarn prebuild'
      - run:
          name: lint/pretty
          command: 'yarn prettier:check'
      - run:
          name: test
          command: 'yarn test:badges'
      - run:
          name: zip dist
          command: 'cd dist && zip -r9 ../lapnap-tail-datasource-dev.zip *'
      - store_artifacts:
          path: lapnap-tail-datasource-dev.zip
      - store_artifacts:
          path: coverage
